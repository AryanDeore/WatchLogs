{
  "name": "watchlogs-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tracer-bullet Chromium extension for WatchLogs issue #26.",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "test:e2e": "node --test --test-timeout=60000 test-e2e/content.spec.js",
    "pretest:e2e": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
